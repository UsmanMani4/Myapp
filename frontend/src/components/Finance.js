import React from 'react';

const Finance = () => {
  return (
    <div style={{ padding: '2rem', background: 'var(--card-bg)', borderRadius: '20px', color: 'var(--text-main)', maxWidth: '600px', margin: '0 auto', boxShadow: '0 8px 25px var(--shadow)' }} className="fade-in">
      <h2>Finance Tracker</h2>
      <p style={{ marginTop: '1rem', opacity: 0.7 }}>Finance tracking coming soon...</p>
    </div>
  );
};

export default Finance;
